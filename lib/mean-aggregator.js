class MeanAggregator {
  /**
   * Aggregate samples in array sample to another array och numValues samples
   * @param samples Input array containing objects { value: <number>, timestamp: <number> }
   * @param numValues Output array containing same kinds of objects as input array but limited to numValues number of elements
   */
  static aggregateSamples (samples, numValues) {
    //TODO
    return samples
  }
}

module.exports = MeanAggregator
